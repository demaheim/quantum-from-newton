"""What lies beyond the ends of the chain.

The interaction sum reaches one link past the last companion, so every run needs
a rule for the outside. Both rules here work the same way: the chain is
*extended* by one ghost companion at each end.
"""
